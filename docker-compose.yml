version: '3.8'

services:
  api:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - .:/app
      - codementor_db:/app/data
    restart: always

volumes:
  codementor_db:
